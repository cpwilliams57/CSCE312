This program is made of 2 classes, parser and writer.

Parser reads in VM code.

Writer translates and writes it.

Pass the fileName into the parser constructor to read.

Pass a parser object into the writer constructor to translate.

Call writer.writeLines() to create file, or printLines() to view
